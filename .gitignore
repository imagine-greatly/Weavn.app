# Dependencies
node_modules/

# Next.js
.next/
out/

# Env (keep .env.example)
.env
.env*.local

# Debug / local
*.tsbuildinfo
.vercel

# Local tooling
.claude/

# OS
.DS_Store
Thumbs.db
node_modules

.vercel
.env*.local

# Supabase CLI local link state (not for commit)
supabase/.temp/

# Local database backups — never commit dumps
backups/

# Scrape fixtures + diagnostic/measurement run output — never commit (may contain
# third-party page HTML; large; regenerated on demand by scripts/captureFixtures.ts etc.)
fixtures/
abscan*.log
diag_*.log
diag_run_*.json
calib*.log
*.log
