# OG Images Required

Three og:image files needed at 1200x630px:

**dashboard.png** — Screenshot the dashboard page `/dashboard`, crop to show the score ring (61/100 CRITICAL) and the first two finding cards. Dark background must be visible. Export at 1200x630px.

**home.png** — Screenshot the homepage `/`, crop to show the scan engine glyph and the two door cards (founder + developer). Export at 1200x630px.

**developer.png** — Screenshot the developer console page `/console`, crop to show the curl command and JSON response terminal. Export at 1200x630px.

Place all three files in this directory as `.png` files. The metadata references them at:
- `/og/dashboard.png`
- `/og/home.png`
- `/og/developer.png`
